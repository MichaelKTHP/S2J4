require_relative '../lib/crypto'

describe "return_an_array_of_hashes?" do
it "should return TRUE when checking for these ashes specs" do
expect(crypto_scrapper[3].keys[0].length < 5).to eq(true)
end
end

